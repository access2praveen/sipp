<?xml version="1.0" encoding="ISO-8859-1" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">
<scenario name="Basic Sipstone UAC">
  <!-- In client mode (sipp placing calls), the Call-ID MUST be         -->
  <!-- generated by sipp. To do so, use [call_id] keyword.                -->
  <send retrans="500">
    <![CDATA[

      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[call_number]
      To: sut <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: [cseq] INVITE
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      X-DolbyVoice-Client-LogId: JustInviteSDPLess-L16multicodec.xml
      X-DolbyVoice-MediaServer-MultiCodec:1  
      Subject: Performance Test
      Content-Type: application/sdp
      Content-Length: [len]

    ]]>
  </send>

  <!--
    * verify that multicodec is requested: CONFSERV113
      [DVCS CONFSERV113 INFO] Client (JustInviteSDPLess-L16multicodec.xml) session (QHm4BZ7HcjSXm) requested multi-codec support in SIP INVITE.
    * verify that L16 is not offered
    * before the fix, this is logged:
      [DVCS CONFSERV117 NOTICE] Multi-codec support requested for incompatible media type (L16) by client (JustInviteSDPLess-L16multicodec.xml) session (QHm4BZ7HcjSXm).
      [DVCS CONFSERV114 NOTICE] Multi-codec support requested by client (JustInviteSDPLess-L16multicodec.xml) session (QHm4BZ7HcjSXm) is only compatible with G711, G722, G722.1, and G722.1C.
    * after the fix, this is logged:
    * verify that this request is rejected: log CONFSERV61, CONFSERV85
      [DVCS CONFSERV61 WARNING] Client (JustInviteSDPLess-L16multicodec.xml), Session (pU0NB0r5jaXtm) does not support any of the codecs provided by the Media Server. Client will be disconnected.
      [DVCS CONFSERV85 INFO] Ending SIP session (JustInviteSDPLess-L16multicodec.xml) with reason (Failure): No match between server SDP offer and client response
  -->

  <recv response="100">
  </recv>

  <recv response="180" optional="true">
  </recv>

  <recv response="183" optional="true">
  </recv>

  <!-- By adding rrs="true" (Record Route Sets), the route sets         -->
  <!-- are saved and used for following messages sent. Useful to test   -->
  <!-- against stateful SIP proxies/B2BUAs.                             -->
  <recv response="200">
    <action>
      <ereg
          regexp="[0-9a-zA-Z]{13}"
          search_in="hdr"
          header="To: "
          check_it="true"
          assign_to="2"/>
      <log message="Matched 'To'=[$2]"/>
    </action> 
  </recv>

  <!-- Packet lost can be simulated in any send/recv message by         -->
  <!-- by adding the 'lost = "10"'. Value can be [1-100] percent.       -->
  <send>
    <![CDATA[

      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: sipp <sip:sipp@[local_ip]:[local_port]>;tag=[call_number]
      To: sut <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: [cseq] ACK
      Contact: sip:sipp@[local_ip]:[local_port]
      Max-Forwards: 70
      Subject: Performance Test
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=user1 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=-
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 99
      a=rtpmap:99 L16/32000/1

    ]]>
  </send>

<recv request="BYE" crlf="true">
  </recv>


  <send retrans="500">
    <![CDATA[

      SIP/2.0 200 OK
      [last_Via:]
      [last_From:]
      [last_To:];tag=[pid]SIPpTag01[call_number]
      [last_Call-ID:]
      [last_CSeq:]
      Contact: <sip:[local_ip]:[local_port];transport=[transport]>
      Content-Type: application/sdp
      Content-Length:0


    ]]>
  </send>



  <!-- This delay can be customized by the -d command-line option       -->
  <!-- or by adding a 'milliseconds = "value"' option here.             -->
  <!-- The 'crlf' option inserts a blank line in the statistics report. -->


  <!-- definition of the response time repartition table (unit is ms)   -->
  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>

  <!-- definition of the call length repartition table (unit is ms)     -->
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000"/>

</scenario>
